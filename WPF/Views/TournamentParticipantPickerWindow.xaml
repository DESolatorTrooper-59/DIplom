<Window x:Class="Tournaments.WPF.Views.TournamentParticipantPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление участниками"
        Width="720"
        Height="620"
        MinWidth="640"
        MinHeight="480"
        Background="{DynamicResource SurfaceBrush}"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="20" Background="{DynamicResource SurfaceBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock x:Name="TitleText"
                 FontSize="28"
                 FontWeight="Bold"
                 Foreground="{DynamicResource TextPrimaryBrush}" />
      <TextBlock x:Name="SubtitleText"
                 Margin="0,8,0,0"
                 Foreground="{DynamicResource TextSecondaryBrush}"
                 TextWrapping="Wrap" />
    </StackPanel>

    <Border Grid.Row="1"
            Margin="0,18,0,0"
            Padding="14"
            Background="{DynamicResource CardBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="16">
      <Grid>
        <TextBox x:Name="SearchTextBox"
                 Height="40"
                 VerticalContentAlignment="Center"
                 TextChanged="SearchTextBox_TextChanged" />
        <TextBlock Margin="12,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextMutedBrush}"
                   IsHitTestVisible="False"
                   Text="Поиск по нику, имени или стране">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Visibility" Value="Collapsed" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                  <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
        </TextBlock>
      </Grid>
    </Border>

    <TextBlock Grid.Row="2"
               x:Name="SummaryText"
               Margin="0,16,0,0"
               Foreground="{DynamicResource TextSecondaryBrush}"
               FontSize="15" />

    <Border Grid.Row="3"
            Margin="0,14,0,0"
            Padding="16"
            Background="{DynamicResource CardBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="16">
      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
          <Border Padding="16"
                  Background="{DynamicResource SubtleCardBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  CornerRadius="16">
            <StackPanel>
              <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Text="Участники турнира"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <Border DockPanel.Dock="Right"
                        Padding="10,5"
                        Background="{DynamicResource ThemeToggleBackgroundBrush}"
                        BorderBrush="{DynamicResource ThemeToggleBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="999">
                  <TextBlock x:Name="RegisteredCountText"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             FontWeight="SemiBold" />
                </Border>
              </DockPanel>

              <TextBlock Margin="0,10,0,0"
                         Foreground="{DynamicResource TextSecondaryBrush}"
                         Text="Удаление выполняется по крестику справа от участника."
                         TextWrapping="Wrap" />

              <ItemsControl x:Name="RegisteredPlayersItemsControl"
                            Margin="0,14,0,0">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Margin="0,0,0,12"
                            Padding="16"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                          <TextBlock Text="{Binding Nickname}"
                                     FontSize="18"
                                     FontWeight="Bold"
                                     Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock Margin="0,6,0,0"
                                     Text="{Binding Details}"
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     TextWrapping="Wrap" />
                        </StackPanel>

                        <Border Grid.Column="1"
                                Margin="16,0,0,0"
                                Padding="10,5"
                                VerticalAlignment="Center"
                                Background="{DynamicResource ThemeToggleBackgroundBrush}"
                                BorderBrush="{DynamicResource ThemeToggleBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="999">
                          <TextBlock Text="{Binding SeedText}"
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     FontWeight="SemiBold" />
                        </Border>

                        <Button Grid.Column="2"
                                Width="38"
                                Height="38"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="18"
                                FontWeight="Bold"
                                Content="×"
                                ToolTip="Удалить участника из турнира"
                                Click="RemovePlayer_Click" />
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>

              <TextBlock x:Name="RegisteredEmptyText"
                         Visibility="Collapsed"
                         Foreground="{DynamicResource TextSecondaryBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Margin="0,16,0,0"
                  Padding="16"
                  Background="{DynamicResource SubtleCardBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  CornerRadius="16">
            <StackPanel>
              <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Text="Можно добавить"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <Border DockPanel.Dock="Right"
                        Padding="10,5"
                        Background="{DynamicResource ThemeToggleActiveBackgroundBrush}"
                        BorderBrush="{DynamicResource ThemeToggleActiveBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="999">
                  <TextBlock x:Name="AvailableCountText"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             FontWeight="SemiBold" />
                </Border>
              </DockPanel>

              <ItemsControl x:Name="PlayersItemsControl"
                            Margin="0,14,0,0">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Button Margin="0,0,0,12"
                            Padding="16"
                            HorizontalContentAlignment="Stretch"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            Click="Player_Click">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                          <TextBlock Text="{Binding Nickname}"
                                     FontSize="20"
                                     FontWeight="Bold"
                                     Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock Margin="0,6,0,0"
                                     Text="{Binding Details}"
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     TextWrapping="Wrap" />
                        </StackPanel>

                        <Border Grid.Column="1"
                                Margin="16,0,0,0"
                                Padding="10,5"
                                VerticalAlignment="Center"
                                Background="{DynamicResource ThemeToggleActiveBackgroundBrush}"
                                BorderBrush="{DynamicResource ThemeToggleActiveBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="999">
                          <TextBlock Text="Добавить"
                                     FontWeight="SemiBold"
                                     Foreground="{DynamicResource TextPrimaryBrush}" />
                        </Border>
                      </Grid>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>

              <TextBlock x:Name="AvailableEmptyText"
                         Visibility="Collapsed"
                         Foreground="{DynamicResource TextSecondaryBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Border>

    <DockPanel Grid.Row="4"
               Margin="0,18,0,0"
               LastChildFill="False">
      <Button DockPanel.Dock="Right"
              Width="140"
              Height="40"
              Content="Закрыть"
              Click="Close_Click" />
    </DockPanel>
  </Grid>
</Window>
